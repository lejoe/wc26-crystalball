/**
 * When the real results (FIXTURES / BRACKET_RESULTS) were last refreshed.
 * ISO 8601 (UTC). The `update-results` skill rewrites this on every run that
 * writes a result. Shown in the header.
 */
export const LAST_RESULTS_UPDATE = '2026-06-22T21:36:00Z'
